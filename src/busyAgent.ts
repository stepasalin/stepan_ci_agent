// import { newLog } from './util/exec';
import { AgentInfoManager, AgentInfo } from './util/AgentInfoManager';

export async function busyAgent(
  infoManager: AgentInfoManager,
  agentInfo: AgentInfo
): Promise<void> {
  // const { logPath } = agentInfo;
  // const newLogEntry = await newLog(logPath);
  // console.log(`lets do something ${newLogEntry} !`);
}
